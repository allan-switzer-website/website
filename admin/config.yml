# config.yml

backend:
  name: git-gateway
  branch: master

media_folder: '/uploads'

collections:
  - name: "posts" # Used in routes, e.g., /admin/collections/blog

    label: "Posts"

    folder: "_posts"

    create: true # Allow users to create new documents in this collection

    fields:
      - label: "Year"
        name: "year"
        widget: "string"
        default: "--"

      - label: "Category"
        name: "category"
        widget: "select"
        options: ["Works on Paper", "Photo Works", "Paintings", "Sculpture", "Reviews", "Exhibitions", "Misc."]

      - label: "Title"
        name: "title"
        widget: "string"

      - label: "Description"
        name: "description"
        widget: "string"

      - label: "Images"
        name: "images"
        widget: "image"
        choose_url: false
        media_library:
          config:
            multiple: true